@using Bb.WebClient.UIComponents


    @if (Menus != null)
    @foreach (DynamicServerMenu menu in Menus)
        @if (menu.ViewGuard)
        {

            <DynamicServerMenuComponent Menu="@menu" />

        }

